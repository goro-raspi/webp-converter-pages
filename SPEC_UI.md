# WebP Converter UI仕様書（Claude Code投入用）

最終更新: 2026-02-23
対象: `projects/webp-converter`（React + Vite）

## 1. 目的
PNG/JPEG → WebP変換ツールを、
- 初見でも使いやすく
- 変換結果を比較しやすく
- 投稿用画像作成に向いた
見た目と導線へ改善する。

---

## 2. デザイン方針
- ダークテーマ基調（現行踏襲）
- 情報の階層を明確化（ヘッダー / 入力 / 設定 / 結果）
- 「操作」と「結果」の分離
- 余白・角丸・タイポを統一してモダン化

### トーン
- 親しみやすい
- 実務ツールらしい見通し
- 過度な装飾はしない

---

## 3. 画面構成（上から順）

### 3.1 ヘッダー
- 左: ごろうロゴ（`public/logo-gorou.*`）
- 右: タイトル + サブ説明
  - タイトル例: `PNG/JPEG → WebP 圧縮ツール`
  - 説明例: `画像はブラウザ内処理。サーバー送信なし。`

### 3.2 ファイル投入セクション
- ドラッグ&ドロップ領域（クリック選択対応）
- 対応形式表示: PNG / JPEG
- 選択件数表示
- 不正形式は即時警告（UIメッセージ）

### 3.3 変換設定セクション（カード）
- 品質スライダー（50〜95）
- 長辺リサイズON/OFF
- 長辺px入力（ON時のみ有効）
- 変換開始ボタン
- ZIP一括DLボタン
- 状態行（処理中/完了/エラー）

### 3.4 プレビューセクション（変換前）
- 見出し: `変換前プレビュー`
- グリッド表示（カード）
  - サムネイル
  - ファイル名
  - 元サイズ
  - 解像度（可能なら）

### 3.5 プレビューセクション（変換後）
- 見出し: `変換後プレビュー`
- グリッド表示（カード）
  - WebPサムネイル
  - ファイル名
  - WebPサイズ
  - 削減率
  - 個別保存ボタン

### 3.6 比較表示（1カード内）
- 可能なら「Before / After」を横並び表示
- モバイルでは縦並びに自動切替

---

## 4. レスポンシブ仕様

### PC（>=1024px）
- 変換前/後のカードを2〜4列グリッド
- 主要操作ボタンは横並び

### タブレット（768〜1023px）
- 2列中心
- ボタン折り返し可

### モバイル（<=767px）
- 1列
- ボタンは縦積み or 横スクロール回避
- サムネ最小高さを確保（視認性優先）

---

## 5. エラーハンドリング表示仕様

### 5.1 エラー種別
- 対応外ファイル
- 画像読み込み失敗
- 変換失敗（canvas/blob化失敗）
- メモリ不足/ブラウザ制約

### 5.2 UI表示
- インラインエラーバナー（赤系）
- ユーザー向け文言（技術用語を避ける）
- 再試行方法を併記

文言例:
- `このファイル形式は対応外です（PNG/JPEGのみ）`
- `変換に失敗しました。品質を下げるか、枚数を減らして再試行してください。`

---

## 6. ローディング/状態表示
- 変換中はボタン非活性 + スピナー/文言表示
- 進捗は最低でも `n / total` を表示
- 完了時は合計削減率サマリー表示

サマリー例:
`完了: 8件 / 12.4MB → 3.1MB（75.0%削減）`

---

## 7. アクセシビリティ要件
- 主要操作にaria-label
- キーボード操作可能（Tab移動）
- コントラスト比を確保
- 画像には適切なalt

---

## 8. 追加オプション（可能なら）
- 変換後のファイル名ルール選択
- note用サムネ比率プリセット（16:9等）
- 「note-intro-thumb.webp」をサンプル表示

---

## 9. 非対象（今回やらない）
- サーバーアップロード機能
- ユーザー認証
- DB保存
- 画像編集機能（トリミング/フィルタ）

---

## 10. 受け入れ条件（Done）
1. 画面がヘッダー/入力/設定/結果で明確に分かれる
2. 変換前後のプレビューが視覚的に比較しやすい
3. モバイルで崩れない
4. 不正入力や失敗時に分かりやすいエラー表示が出る
5. `pnpm build` が通る

---

## 11. 実装メモ（Claude Code向け）
- 既存機能（品質・リサイズ・ZIP）は維持
- 依存追加は最小限
- 既存のロゴ探索ロジックは活かしてOK
- 変更後に `pnpm build` 実行して報告
